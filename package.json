{
  "name": "sst-crud-api-dynamodb",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.91.0"
  },
  "dependencies": {
    "@aws-cdk/core": "1.91.0",
    "@serverless-stack/cli": "0.9.8",
    "@serverless-stack/resources": "0.9.8",
    "aws-sdk": "^2.853.0",
    "uuid": "^8.3.2"
  }
}
